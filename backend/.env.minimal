# ============================================================================
# REQUIRED VARIABLES (Must be set)
# ============================================================================

# Database Configuration
DATABASE_URL=postgresql://neondb_owner:npg_VY0ukOzR8jmK@ep-shy-tree-ah0y8hv8-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# JWT Authentication (MUST be 32+ characters)
JWT_SECRET=your_super_secret_jwt_key_change_this_to_something_secure_and_random_32plus

# Master Encryption Key (MUST be 32+ characters)
MASTER_ENCRYPTION_KEY=your_master_encryption_key_also_needs_to_be_32_characters_or_more

# ============================================================================
# OPTIONAL VARIABLES (Have defaults)
# ============================================================================

# Application Settings
APP_NAME=CyborgDB
APP_VERSION=1.0.0
ENVIRONMENT=development
DEBUG=true

# Server
HOST=0.0.0.0
PORT=8000

# Database Pool Settings
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=30
DB_ECHO=false

# Redis (Optional)
REDIS_URL=redis://localhost:6379/0
REDIS_ENABLED=false

# JWT Settings
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24
REFRESH_TOKEN_EXPIRATION_DAYS=30

# CyborgDB (Optional - can be set later)
CYBORGDB_API_KEY=placeholder_get_real_key_from_cyborgdb_dashboard
CYBORGDB_ENDPOINT=https://api.cyborg.co
CYBORGDB_TIMEOUT=30

# Embedding Service
EMBEDDING_SERVICE_URL=http://localhost:8001
EMBEDDING_DIMENSION=384

# CORS
CORS_ORIGINS=http://localhost:3000

# Logging
LOG_LEVEL=INFO

# Rate Limiting
RATE_LIMIT_ENABLED=true
MAX_LOGIN_ATTEMPTS=5
RATE_LIMIT_WINDOW_SECONDS=300

# Document Processing
MAX_FILE_SIZE_MB=50
ALLOWED_FILE_TYPES=pdf,docx,txt
CHUNK_SIZE=512
CHUNK_OVERLAP=50

# Search Configuration
DEFAULT_TOP_K=10
MAX_TOP_K=100
SEARCH_TIMEOUT_SECONDS=30

# Storage
STORAGE_BACKEND=local
LOCAL_STORAGE_PATH=./storage/documents
