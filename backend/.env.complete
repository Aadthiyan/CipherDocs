# ============================================================================
# REQUIRED VARIABLES (Must be set)
# ============================================================================

# Database Configuration
DATABASE_URL=postgresql://neondb_owner:npg_VY0ukOzR8jmK@ep-shy-tree-ah0y8hv8-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# JWT Authentication (MUST be 32+ characters)
JWT_SECRET=dev_jwt_secret_change_in_production_use_strong_random_key_12345678

# Master Encryption Key (MUST be 32+ characters)
MASTER_ENCRYPTION_KEY=dev_master_key_32_bytes_base64_change_in_production_abc

# ============================================================================
# CYBORGDB CONFIGURATION
# ============================================================================

# Your CyborgDB API Key
CYBORGDB_API_KEY=cyborg_917046e34dfd49e08aa7531af35396dc

# CyborgDB Endpoint (use cyborgdb:8002 inside Docker, localhost:8002 from host)
CYBORGDB_ENDPOINT=http://cyborgdb:8002

# CyborgDB Timeout
CYBORGDB_TIMEOUT=30

# ============================================================================
# OPTIONAL VARIABLES (Have defaults)
# ============================================================================

# Application Settings
APP_NAME=CyborgDB
APP_VERSION=1.0.0
ENVIRONMENT=development
DEBUG=true

# Server
HOST=0.0.0.0
PORT=8000

# Database Pool Settings
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=30
DB_ECHO=false

# Redis (Optional)
REDIS_URL=redis://localhost:6379/0
REDIS_ENABLED=false

# JWT Settings
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24
REFRESH_TOKEN_EXPIRATION_DAYS=30

# Embedding Service
EMBEDDING_SERVICE_URL=http://localhost:8001
EMBEDDING_DIMENSION=384

# CORS
CORS_ORIGINS=http://localhost:3000

# Logging
LOG_LEVEL=INFO

# Rate Limiting
RATE_LIMIT_ENABLED=true
MAX_LOGIN_ATTEMPTS=5
RATE_LIMIT_WINDOW_SECONDS=300

# Document Processing
MAX_FILE_SIZE_MB=50
ALLOWED_FILE_TYPES=pdf,docx,txt
CHUNK_SIZE=512
CHUNK_OVERLAP=50

# Search Configuration
DEFAULT_TOP_K=10
MAX_TOP_K=100
SEARCH_TIMEOUT_SECONDS=30

# Storage
STORAGE_BACKEND=local
LOCAL_STORAGE_PATH=./storage/documents
